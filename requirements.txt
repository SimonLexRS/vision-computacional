# Dependencias del proyecto (versiones exactas del entorno usado).
# pip install -r requirements.txt
#
# Nota: torch/torchvision se fijan sin sufijo de CUDA — el wheel por
# defecto de PyPI funciona en CPU. Para GPU NVIDIA (CUDA 13.0), instalar con:
#   pip install torch==2.13.0 torchvision==0.28.0 --index-url https://download.pytorch.org/whl/cu130
torch==2.13.0
torchvision==0.28.0
numpy==2.5.1
pandas==3.0.5
scikit-learn==1.9.0
matplotlib==3.11.1
seaborn==0.13.2
pillow==12.3.0
tqdm==4.70.0
jupyter==1.1.1
ipykernel==7.3.0
# Exportación del modelo a ONNX para la demo web (src/export_onnx.py)
onnx==1.22.0
onnxruntime==1.28.0
onnxscript==0.7.1
# Edge detection en vivo con cámara (src/edge_detection.py)
opencv-python==4.12.0.88
